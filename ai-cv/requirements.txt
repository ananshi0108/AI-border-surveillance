# Core
numpy>=1.24
opencv-contrib-python>=4.9.0.80,<5
pyyaml>=6.0
tqdm>=4.66
requests>=2.31
Pillow>=10.0
scipy>=1.11

# Detection & tracking
ultralytics>=8.2.0          # YOLOv8/v11, includes built-in ByteTrack/BoT-SORT
deep-sort-realtime>=1.3.2   # alternative tracker (DeepSORT)
supervision>=0.21.0         # tracking/annotation utilities

# ANPR
easyocr>=1.7.1              # OCR engine (GPU/CPU). pytesseract is a lighter alternative:
# pytesseract>=0.3.10       # requires system package: sudo apt install tesseract-ocr

# Low-light enhancement / deep learning backbone
torch>=2.2.0
torchvision>=0.17.0

# Edge export & optimisation
onnx>=1.16.0
onnxruntime>=1.18.0         # onnxruntime-gpu on edge boxes with NVIDIA GPUs
# tensorrt                  # install via NVIDIA's own wheel/SDK on the target Jetson/edge device
